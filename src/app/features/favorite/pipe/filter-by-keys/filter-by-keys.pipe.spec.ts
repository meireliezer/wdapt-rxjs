import { FilterByKeysPipe } from './filter-by-keys.pipe';

describe('FilterByKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
